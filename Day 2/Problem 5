Commands for:

See all nodes
See pod details
Check logs
Exec into pod

Solution:- 
kubectl get nodes
kubectl describe pod <pod-name>
kubectl logs <pod-name>
kubectl exec -it <pod-name> -- /bin/bash

# Bonus (Very Important)
kubectl get pods -o wide

Shows node placement
